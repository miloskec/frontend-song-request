# Auth Mock Module Spec

## Goal
Provide realistic but frontend-safe auth flows for demo purposes.

## Requirements
- login screen should work in mock mode
- me endpoint should return authenticated user shape
- auth store should stay lightweight
- token handling can be simulated in-memory or local storage during demo phase

## Non-goal
No real security implementation in this repo.
